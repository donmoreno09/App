/**
 * Interactions are handled within the Map component.
 * This only serves to provide a default mode.
 *
 * Nevertheless, this can be implemented for interactions.
 * Check a commit message with the "temp" label if
 * interactions are needed to be handled here.
 */

import QtQuick 6.8
import QtLocation 6.8
import QtPositioning 6.8
import Qt.labs.animation 6.8

BaseMode {
    id: root
    type: "interaction"
}
